# Script to upload GSM materials to Contour GraphQL API

## Usage

To get help:

```sh
python3 gsm.py -h
```

Example:

```sh
python3 gsm.py -s ~/gsm_files -u olya -p hammer69 http://contour-dev-01.contour.net:5000
```
